# Tasks

## 1. .env 配置解析

- [x] 1.1 在 `main.ahk` 中实现 `.env` 轻量解析（跳过空行/`#` 注释、按第一个 `=` 分割、两侧 trim、忽略未知键），验证：创建含注释/空行/未知键/带空格键值的 `.env`，脚本启动无报错且取值正确
- [x] 1.2 实现 `HOTKEY` 读取与默认值回退（文件缺失、`HOTKEY` 缺失或为空 → `MButton`），验证：删除 `.env` 后脚本正常运行且热键为中键
- [x] 1.3 实现 `MODE` 读取与校验（`hold`/`tap` 大小写不敏感；缺失、为空或非法值 → `hold`），验证：分别写入 `MODE=tap`、`MODE=HOLD`、`MODE=press`、无 MODE 四种情况，行为符合规格

## 2. 热键注册与模式状态机

- [x] 2.1 用 `Hotkey()` 按 `.env` 的 `HOTKEY` 值动态注册触发热键（hold 模式注册 Down/Up 成对热键，tap 模式仅注册 Down），注册失败 try/catch 回退 `MButton` 并 TrayTip 提示，验证：`HOTKEY=F1` 时按 F1 触发、非法热键串时回退中键并有提示
- [x] 2.2 实现模式状态机：`EnterSpeedMode()` / `ExitSpeedMode()` 集中管理状态、速度变量与定时器清理（杀停），验证：代码审查确认所有退出路径（hold Up、tap 切换）都经过 `ExitSpeedMode()`

## 3. 滚轮拦截与速度映射

- [x] 3.1 实现 `*WheelUp::` / `*WheelDown::` 热键：速度模式内更新有符号速度（WheelDown 加、WheelUp 减，过零反转），非速度模式内放行原生滚动，验证：普通模式滚轮行为与原生一致
- [x] 3.2 用 `#InputLevel`/`SendLevel` 分层，确保脚本合成滚轮事件不被自身热键再次拦截，验证：速度模式持续滚动期间无自吞循环、速度不因合成事件被意外改变
- [x] 3.3 实现 `SetTimer` 滚动 tick：按当前速度每 tick 发送整数行数 + 小数累积器（亚行精度），速度为零不发送，验证：低速拨动一下页面缓慢平滑滚动、高速时滚动明显加快

## 4. 集成验收

- [x] 4.1 hold 模式端到端验证：按住中键拨动滚轮加速/反向减速/过零反转，松开瞬间滚动立即停止且滚轮恢复原生行为
- [ ] 4.2 tap 模式端到端验证：按一下进入速度模式（松开不退出），再次按下立即杀停退出
- [x] 4.3 无 `.env` 冷启动验证：删除 `.env` 后运行脚本，默认中键 + hold 模式一切正常，无报错弹窗
